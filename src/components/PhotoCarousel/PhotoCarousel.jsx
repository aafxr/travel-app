import {motion} from "framer-motion";
import {animationVariant} from "./animationVariant";

export default function PhotoCarousel({startValue,urls, ...props}){

    return (
        <motion.div
        variants={animationVariant}
        >

        </motion.div>
    )
}